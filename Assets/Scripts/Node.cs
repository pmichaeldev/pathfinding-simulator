using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Node : MonoBehaviour
{

    #region ABOUT
    /**
     * This script handles basic Node (on graph) functionality.
     * Each node will have an Array of Nodes for its neighbors, and this is public.
     **/
    #endregion

    #region VARIABLES
    public GameObject[] neighborNodes;
    #endregion

    // Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
